//creating Array
let values = [2,6,4,8,9]

console.log(values);  //print all the values in an Array

console.log(values.length); // to get length of the Array

for(let i=0;i<values.length;i++)  // iterating all the values
{
    console.log(values[i]);
}

// toString() method return String format of Array separted by comma(,)
let res = values.toString()

console.log(res); // 2,6,4,8,9

